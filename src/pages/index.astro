---
// import { defineConfig } from 'astro/config';
// import config from '../astro.config.mjs';
// import { getRelativeLocaleUrl } from 'astro:i18n';

// Fast direct redirect, but hardcoded locale
// For config-driven redirect, use:
// return Astro.redirect(getRelativeLocaleUrl('fr' as const));

return Response.redirect('/fr', 307);
---
